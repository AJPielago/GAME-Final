<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Continue Your Quest - CodeQuest</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <script src="/js/music.js" defer></script>
    <script src="/js/auth-utils.js" defer></script>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar">
        <div class="nav-container">
            <div class="nav-brand">
                <a href="/" class="brand-link">
                    <img src="/images/logo.gif" alt="CodeQuest Logo" class="brand-logo">
                    <span class="brand-text">CodeQuest</span>
                </a>
            </div>
            
            <div class="nav-menu" id="navMenu">
                <a href="/" class="nav-link">Home</a>
                <a href="/leaderboard" class="nav-link">Leaderboard</a>
                <a href="/auth/login" class="nav-link">Login</a>
                <a href="/auth/register" class="nav-link register-link">Join Quest</a>
            </div>
            
            <div class="nav-toggle" id="navToggle">
                <span class="bar"></span>
                <span class="bar"></span>
                <span class="bar"></span>
            </div>
        </div>
    </nav>

    <!-- Music Button -->
    <button id="toggleMusic" title="Music On" aria-pressed="true" style="position:fixed;top:85px;left:15px;width:48px;height:48px;padding:0;margin:0;border:none;cursor:pointer;z-index:10000;background:rgba(0,0,0,0.3);border-radius:8px;font-size:28px;line-height:48px;text-align:center;color:#fff;box-shadow:0 2px 8px rgba(0,0,0,0.3);">🔊</button>

    <!-- Main Content -->
    <main class="main-content" style="background: url('/images/login.gif') center center / cover; min-height: 100vh; display: flex; align-items: center; justify-content: center; padding: 20px;">
        <section class="pixel-auth-section">
            <div class="pixel-window" style="background: #f0f0f0; border: 4px solid #000; box-shadow: inset -2px -2px #808080, inset 2px 2px #ffffff; max-width: 400px; padding: 20px; font-family: 'Courier New', monospace;">
                <div class="pixel-header" style="text-align: center; margin-bottom: 20px; border-bottom: 2px solid #000; padding-bottom: 10px;">
                    <h1 style="font-size: 18px; font-weight: bold; margin: 0; letter-spacing: 1px; text-transform: uppercase; color: #000;">
                        CONTINUE YOUR<br>JOURNEY
                    </h1>
                </div>
                    
                <form class="pixel-form" action="/auth/login" method="POST" novalidate style="display: flex; flex-direction: column; gap: 15px;" onsubmit="if(window.AuthUtils) window.AuthUtils.clearGameData();">
                    <div class="pixel-input-group">
                        <input 
                            type="text" 
                            id="login" 
                            name="login" 
                            placeholder="Email"
                            value="<%= formData ? formData.login : '' %>"
                            required
                            autocomplete="username"
                            style="width: 100%; padding: 8px; border: 2px inset #c0c0c0; font-family: 'Courier New', monospace; font-size: 14px; background: white;"
                        >
                    </div>
                    
                    <div class="pixel-input-group">
                        <input 
                            type="password" 
                            id="password" 
                            name="password" 
                            placeholder="Password"
                            required
                            autocomplete="current-password"
                            style="width: 100%; padding: 8px; border: 2px inset #c0c0c0; font-family: 'Courier New', monospace; font-size: 14px; background: white;"
                        >
                        <div style="margin-top: 8px; display: flex; align-items: center; gap: 6px;">
                            <input 
                                type="checkbox" 
                                id="showPassword" 
                                style="cursor: pointer; width: 14px; height: 14px;"
                                onchange="togglePasswordVisibility('password')"
                            >
                            <label for="showPassword" style="font-size: 12px; color: #404040; cursor: pointer; user-select: none;">Show Password</label>
                        </div>
                    </div>
                    
                    <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
                        <div class="pixel-errors" style="background: #ffcccc; border: 2px solid #ff0000; padding: 10px; margin: 10px 0; font-family: 'Courier New', monospace;">
                            <% errors.forEach(error => { %>
                                <div style="color: #cc0000; font-size: 12px; margin: 2px 0;">
                                    ⚠ <%= error.msg %>
                                </div>
                            <% }); %>
                        </div>
                    <% } %>
                    
                    <div class="pixel-button-container" style="display: flex; justify-content: center; margin-top: 20px;">
                        <button type="submit" style="background: #c0c0c0; border: 2px outset #c0c0c0; padding: 10px 30px; font-family: 'Courier New', monospace; font-size: 14px; font-weight: bold; cursor: pointer; text-transform: uppercase; letter-spacing: 1px; box-shadow: 2px 2px 4px rgba(0,0,0,0.3);" 
                                onmousedown="this.style.border='2px inset #c0c0c0'" 
                                onmouseup="this.style.border='2px outset #c0c0c0'" 
                                onmouseleave="this.style.border='2px outset #c0c0c0'">
                            Continue
                        </button>
                    </div>
                </form>
                
                <div class="pixel-footer" style="text-align: center; margin-top: 15px; padding-top: 15px; border-top: 1px solid #808080;">
                    <p style="font-size: 12px; margin: 0; color: #404040;">
                        New to CodeQuest? <a href="/auth/register" style="color: #0000ff; text-decoration: underline;">Start Adventure</a>
                    </p>
                </div>
            </div>
        </section>
    </main>

    <!-- Background Music -->
    <audio id="backgroundMusic" loop>
        <source src="/audio/music.mp3" type="audio/mpeg">
        <source src="/audio/music.ogg" type="audio/ogg">
    </audio>

    <script src="/js/main.js"></script>
    <script>
        // Auto-focus on login field
        document.addEventListener('DOMContentLoaded', function() {
            document.getElementById('login').focus();
        });
        
        // Toggle password visibility
        function togglePasswordVisibility(fieldId) {
            const field = document.getElementById(fieldId);
            const checkbox = document.getElementById('showPassword');
            
            if (checkbox.checked) {
                field.type = 'text';
            } else {
                field.type = 'password';
            }
        }
    </script>
</body>
</html>

