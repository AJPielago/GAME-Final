<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background: #000000;
            font-family: Arial, sans-serif;
            overflow: hidden;
        }
        
        .navbar {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: white;
            padding: 10px 20px;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 100;
            height: 40px;
            box-sizing: border-box;
            border-bottom: 3px solid #FF6B6B;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .navbar h1 {
            margin: 0;
            font-size: 18px;
            line-height: 20px;
            color: #FF6B6B;
        }
        
        .navbar .nav-links {
            display: flex;
            gap: 15px;
            align-items: center;
        }
        
        .navbar .nav-links a {
            color: white;
            text-decoration: none;
            font-size: 14px;
        }
        
        .navbar .nav-links a:hover {
            color: #FF6B6B;
        }
        
        .navbar .username {
            color: #FFD700;
            font-weight: bold;
            font-size: 14px;
        }
        
        #gameCanvas {
            display: block;
            background: #1a1a1a;
            image-rendering: pixelated;
            image-rendering: -moz-crisp-edges;
            image-rendering: crisp-edges;
        }
        
        #loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            color: white;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 1000;
            font-family: Arial, sans-serif;
        }
        
        #loading-screen h2 {
            margin-bottom: 20px;
            color: #FF6B6B;
            font-size: 32px;
            text-shadow: 0 0 10px rgba(255, 107, 107, 0.5);
        }
        
        #loading-screen p {
            margin: 10px 0;
            font-size: 16px;
            color: #FFFFFF;
        }
        
        .spinner {
            border: 4px solid #333;
            border-top: 4px solid #FF6B6B;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            animation: spin 1s linear infinite;
            margin: 20px 0;
            box-shadow: 0 0 20px rgba(255, 107, 107, 0.3);
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .battle-icon {
            font-size: 64px;
            margin-bottom: 20px;
            animation: pulse 1.5s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.1); opacity: 0.8; }
        }
    </style>
</head>
<body>
    <div class="navbar">
        <h1>⚔️ Monster Battle Arena - Quest 11</h1>
        <div class="nav-links">
            <a href="/dashboard">Dashboard</a>
            <a href="/profile">Profile</a>
            <% if (user.role === 'admin') { %>
            <a href="/admin/analytics" style="color: #FFD700; font-weight: bold;">Admin</a>
            <% } %>
            <span class="username">Welcome, <%= user.username %>!</span>
            <form action="/auth/logout" method="POST" style="display: inline;">
                <button type="submit" style="background: none; border: 1px solid white; color: white; padding: 5px 10px; border-radius: 4px; cursor: pointer;">Logout</button>
            </form>
        </div>
    </div>
    
    <canvas id="gameCanvas"></canvas>
    
    <div id="loading-screen">
        <div class="battle-icon">⚔️</div>
        <h2>Loading Battle Arena...</h2>
        <div class="spinner"></div>
        <p>Preparing monsters and weapons...</p>
        <p style="color: #FF6B6B; font-weight: bold; margin-top: 20px;">Get ready for battle!</p>
    </div>
    
    <!-- Pass current user game data to map3 -->
    <script>
        window.gameData = {
            user: {
                id: '<%= user._id %>',
                username: '<%= user.username %>',
                characterType: '<%= user.characterType || "knight" %>',
                characterAvatar: '<%= user.characterAvatar || "/images/characters/player/Knight.png" %>',
                level: <%= user.level || 1 %>,
                experience: <%= user.experience || 0 %>,
                pixelCoins: <%= user.pixelCoins || 0 %>,
                badges: <%- JSON.stringify(user.badges || []) %>,
                role: '<%= user.role || "player" %>'
            }
        };
        console.log('✅ Map3: Game data loaded with user progress:', {
            username: window.gameData.user.username,
            level: window.gameData.user.level,
            experience: window.gameData.user.experience,
            pixelCoins: window.gameData.user.pixelCoins,
            badges: window.gameData.user.badges.length
        });
    </script>
    
    <!-- Load audio manager -->
    <script src="/js/audio-manager.js?v=<%= Date.now() %>"></script>
    
    <!-- Load map3 rendering module FIRST -->
    <script src="/js/map3_render.js?v=<%= Date.now() %>"></script>
    
    <!-- Load collision debug system -->
    <script src="/js/collision-debug-new.js?v=<%= Date.now() %>"></script>
    
    <!-- Load map3 game system -->
    <script src="/js/game_map3.js?v=<%= Date.now() %>"></script>
</body>
</html>
